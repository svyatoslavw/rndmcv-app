import { Metadata } from "next"

import { ResumeCreatePage } from "@/pages_"

export const metadata: Metadata = {
  title: "Create Resume"
}

export default function ResumeCreate() {
  return <ResumeCreatePage />
}
